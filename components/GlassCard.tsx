/**
 * GlassCard — legacy re-export shim.
 * Actual implementation lives in Card.tsx (Obsidian Protocol v2.0).
 * All existing imports of GlassCard continue to work unchanged.
 */
export { default, GlassCard } from './Card';
