/**
 * Hoy.tsx — Re-export del nuevo DashboardScreen.
 *
 * Mantiene la ruta de importación existente en App.tsx intacta.
 * El archivo legacy original se preserva en useHoyLogic.ts como referencia.
 */
export { default } from './DashboardScreen';
